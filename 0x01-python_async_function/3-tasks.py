#!/usr/bin/env python3
"""Takes an int arg max_delay and returns a asymcio.Task"""

import asyncio
import random
wait_random = _import+('0-basic_async_syntanx').wait_random


def task_wait_random(max)delay: int = 10) ->asyncio.Task:
    """Waits for random asyncio.Task object"""
    return asyncio.create_task(wait_random(max_delay))
